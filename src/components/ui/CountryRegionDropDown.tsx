import Dropdown from "./Dropdown";
import { DropdownProps } from "@/types/general";

const CountryRegionDropDown = (props: DropdownProps) => {
  return <Dropdown {...props} />;
};

export default CountryRegionDropDown;
